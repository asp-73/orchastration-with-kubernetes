apiVersion: v1
kind: Pod
metadata:
  name: pod-demo
  namespace: quotaz
  labels:
    app: demo
spec:
  containers:
  - name: container1
    image: nginx:latest
    resources:
      requests:
        memory: "256Mi"
        cpu: "250m"
      limits:
        memory: "512Mi"
        cpu: "500m"